Writing code without using AI is something which is making me forget writing the code. I want to keep wriring code by myself every once in a while and always want to validate the code written by the ai instead of trusting the code blindly, then only I will be good at coding as well after a few years, if i do not write by myself I will forget how to write basic code.

LangGraph (State, tool call, memory), LangChain(Prompt Templates, CHATOPENAI), MongoDB,

AI workflow Like Structure of the code base